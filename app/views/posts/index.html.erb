

<h1>Posts</h1>


<% @posts.each do |post| %>
  <div class="post">
    <h3><%= post.title %></h3>
    <h5><%= post.body %></h5>
  <%= image_tag post.image %>
    <%= link_to post.title, topic_post_comments_path(@topic,post) %>
    <%= button_to "Edit Post", edit_topic_post_path(@topic,post), :method => :get %>

  </div>



<% end %>


  <%= button_to "New Post", new_topic_post_path, :method => :get %>
